class BaseService:
    """
    Base class representing a core service layer.
    Future concrete logic services (e.g., threat intelligence ingestion,
    agent orchestration workflows, RAG search engines) will inherit from this class
    to maintain system architecture patterns.
    """
